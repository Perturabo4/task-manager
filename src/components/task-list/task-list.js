import React from 'react'

import Card from '../card';

const TaskList = ({tasks}) => {
    return (
        <>
           {
                tasks.map((task) => {
                
                });
           }
        </>
    )
}

export default TaskList;